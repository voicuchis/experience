1. How to change default start directory for Jupyter

Go to where the lnk(shortcut) file was created
Right-Click on the lnk file -> Properties -> Target.
Change the last value there, which is something like %USER_NAME%, to whatever the path you want.